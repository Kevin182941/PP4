@model IEnumerable<PP4.Services.MVC_Service.ServiceReference1.Purchase>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="row" style="text-align:right" ;paddinng:5px;>
    <a href="~/Purchase/New" class="btn btn-primary">New</a>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_Purchase)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_Batch)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_Person)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date_Purchase)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ID_Purchase)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ID_Batch)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ID_Person)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Date_Purchase)
    </td>
    <td>
        <a class="btn btn-default" href="~/Purchase/Edit/@item.ID_Purchase">Edit</a>
        <tf></tf>
        <a class="btn btn-danger" href="~/Purchase/Delete/@item.ID_Purchase">Delete</a>
    </td>
</tr>
}

</table>
