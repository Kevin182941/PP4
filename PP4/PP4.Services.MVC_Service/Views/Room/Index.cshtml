@model IEnumerable<PP4.Services.MVC_Service.ServiceReference1.Room>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="row" style="text-align:right" ;paddinng:5px;>
    <a href="~/Room/New" class="btn btn-primary">New</a>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_Room)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Capacity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.State)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ID_Room)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Description)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Capacity)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.State)
    </td>
    <td>
        <a class="btn btn-default" href="~/Room/Edit/@item.ID_Room">Edit</a>
        <tf></tf>
        <a class="btn btn-danger" href="~/Room/Delete/@item.ID_Room">Delete</a>
    </td>
</tr>
}

</table>
